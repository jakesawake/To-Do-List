import { addNoteBtn } from "./main";

const addNote = function () {
  addNoteBtn.addEventListener("click", (e) => {
    const clicked = e.target;
    // call function to create item here
  });
};

export { addNote };
